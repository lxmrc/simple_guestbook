<%= form_with(model: guestbook) do |form| %>
  <% if guestbook.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(guestbook.errors.count, "error") %> prohibited this guestbook from being saved:</h2>

      <ul>
        <% guestbook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
